// just a test for BasicMao, no longer used
import React from "react";
import BasicMap from "../../3-party-map/map";

const MapView = (props) => (
  <div>
    <BasicMap display={true} />
  </div>
);

export default MapView;
